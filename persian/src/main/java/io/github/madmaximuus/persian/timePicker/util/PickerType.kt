package io.github.madmaximuus.persian.timePicker.util

/**
 * Enum class representing the types of pickers available.
 *
 * @property WHEEL A picker type that uses a wheel for selection.
 * @property DIAL A picker type that uses a dial for selection.
 */
enum class PickerType { WHEEL, DIAL }